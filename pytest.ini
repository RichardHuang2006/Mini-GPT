[pytest]
# Put the repo root on sys.path so tests import `config` and `mini_gpt`
# directly, without an install or per-test path hacking.
pythonpath = .
testpaths = tests
addopts = -ra
markers =
    cuda: test requires a CUDA device (skipped automatically when unavailable)
