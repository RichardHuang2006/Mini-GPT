[pytest]
# Put the repo root on sys.path so tests import `mini_gpt` directly, with no
# install step — the same arrangement the sibling Mini-vLLM repo uses.
pythonpath = .
testpaths = tests
addopts = -ra
