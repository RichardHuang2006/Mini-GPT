"""Mini-GPT — a from-scratch GPT pipeline, one module per concept.

config, tokenizer, data, model, train, generate, kernels, posttrain, evaluate.
Nothing is re-exported; import from the module itself.
"""
